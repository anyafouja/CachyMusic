name: Cachy Music Bot
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
  schedule:
    - cron: "15 */5 * * *"
concurrency:
  group: cachy-bot
  cancel-in-progress: true
jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt update -qq && sudo apt install -y -qq ffmpeg libopus0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -q -r requirements.txt
      - run: |
          echo "DISCORD_TOKEN=${{ secrets.DISCORD_TOKEN }}" > .env
          echo "LAVALINK_URI=${{ secrets.LAVALINK_URI || 'lavalink.jirayu.net:13592' }}" >> .env
          echo "LAVALINK_PASSWORD=${{ secrets.LAVALINK_PASSWORD || 'youshallnotpass' }}" >> .env
          echo "LAVALINK_SECURE=${{ secrets.LAVALINK_SECURE || 'false' }}" >> .env
      - run: python3 main.py
